{
  "name": "code-backend",
  "version": "1.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "lint": "node --check server.js && node --check config/env.js && node --check config/db.js && node --check routes/users.js && node --check routes/auth.js && node --check models/User.js && node --check middleware/errorHandler.js && node --check scripts/seed.js && node --check scripts/backup.js",
    "test": "npm run lint",
    "db:seed": "node scripts/seed.js",
    "db:backup": "node scripts/backup.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "express-rate-limit": "^8.3.1",
    "helmet": "^8.1.0",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^7.0.0",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
